# 样本对话

七个场景：前六个对应需求文档第 7 节，S7 是决定被推翻，业务换成中性的记账小程序。每个目录三份文件：

- `input.json`：统一消息格式的输入
- `expected.json`：预期的最终结果，第 5、6 步的检查都以它为准
- `ideal.json`：手写的理想整理结果，第 5 步用它单独测记忆引擎

`expected.json` 里任务按关键词匹配（`match` 里任意一个词出现在任务名里即可），因为大模型起的任务名每次会有出入。
